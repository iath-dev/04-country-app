{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "ng serve + debug",
      "type": "pwa-chrome",
      "request": "launch",
      "preLaunchTask": "npm: start",
      "url": "http://localhost:4200/",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
